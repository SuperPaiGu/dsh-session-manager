# v0.4.0 — 移除删除功能，只做恢复

## 为什么移除删除

删除的做法是「把会话文件夹送进系统回收站 **+** 把它从归档名单里摘掉」。

**这个组合本身就是危险的**：摘掉归档名单里的记录 = **取消隐藏** = 该会话重新出现在侧栏。所以只要有任何一条路径「名单摘掉了、但文件夹其实没被删」，用户看到的就是**删除变成了恢复**——点删除，会话回到工作区。

实测确实会复现（磁盘上没有文件夹的会话就走这条路径）。

**会话的生命周期归官方产品管**，归档视图不是跟它抢这件事的地方。所以删除整个去掉了。

## 现在的面板

标签栏 **对话 | 轨迹 | 归档**，「归档」面板：

- **只列出真正被归档、且日志还在磁盘上的会话**
- 每行：标题、工作目录、最后活动时间 + 一个 **恢复** 按钮
- 勾选后可 **批量恢复**，支持全选
- 运行中的会话带「运行中」标记

**没有删除按钮，也不会有。**

## 恢复是怎么做的

DSH 0.1.5 的归档是**单向**的：`WorkspaceRegistry` 只公开 `archiveSession()`，官方 README 明确写着「no unarchive action exists yet」——**取消归档在官方接口层面不存在**。

本插件通过 DSH 的 **storage 域**直接改写归档名单（`~/.dsh/storages/workspace.json` 的 `archivedSessionIds`）。归档从不碰 workspace 成员记账，所以摘掉 id，会话就回到原位。写入后**回读校验**，条目还在就报错，不假装成功。

代价（已做进产品行为）：

- **官方侧栏需刷新一次页面**才看到恢复结果——运行中的注册表持有内存副本，直接改盘不会通知它。面板会提示这一点。
- 用的是标注为**诊断用途**的内部面，DSH 升级后可能变化，届时恢复会**明确报错**。

## 安装

```sh
dsh plugin --profile web add ./dsh-session-manager-0.4.0.tgz
```

然后**必须**把 `"dsh-session-manager"` 加进 `%DSH_HOME%\profiles\web\package.json` 的 `dsh.profile.bundles` 数组，再重启 DSH Web 服务。

> `dsh plugin add` 只把包装进 node_modules，**不会自动启用**。

## 破坏性变更（相对 0.3.0）

- `POST /session-manager/delete` 端点**已移除**（现在返回 405）
- `GET /session-manager/archived` 响应简化为 `{ ok, source, ids }`
- 面板不再显示删除按钮

## 保留

- 左右分栏手柄的安全内边距（比例内边距，逐像素实测过）
- 面板每 4 秒重读一次权威归档集合，磁盘变了自动纠正
- 路径编码的只读实证脚本（`tests/encoding-check.mjs`）

## 已知限制

- **只做恢复，不做删除**
- 只显示「日志还在磁盘上」的归档会话；名单里那些文件早已被删的历史记录不显示
- 恢复后侧栏需刷新一次页面
- 两个实例共用一个 home 目录时，归档名单有两份内存副本，**归档管理建议固定在一个实例里做**（README 有详细说明）

## 实测

```
端点：/archived 200、/restore 400(存在)、/delete 405(已移除)
面板：无任何删除按钮，底部只有「恢复选中」
全选 + 恢复选中：4 个会话一次恢复成功，无错误
编码实证：10 个真实项目目录全部往返一致
```
